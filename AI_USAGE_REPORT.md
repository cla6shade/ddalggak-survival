# 딸깍 서바이벌 AI 활용 개발 보고서

작성일: 2026년 8월 7일

프로젝트명: 딸깍 서바이벌

작성 범위: 에셋 생성기, 웹 게임 클라이언트, 개발 및 검증 과정


## 1. 작성 목적

본 문서는 「딸깍 서바이벌」 프로젝트를 제작하는 과정에서 인공지능을 어떤 목적으로 활용하였으며, 인공지능이 관여한 부분과 일반 프로그램으로 처리한 부분이 어떻게 구분되는지를 설명하기 위해 작성하였다.

이 프로젝트에서는 인공지능을 크게 두 가지 용도로 활용하였다. 첫 번째는 게임에 필요한 이미지 원본을 생성하고 수정하는 용도이며, 두 번째는 에셋 생성기와 웹 게임의 프로그램 구조를 설계하고 코드를 작성·검토하는 개발 보조 용도이다.

이미지 생성 과정에서는 OpenAI Codex CLI의 이미지 생성 기능을 사용하였다. 반면 이미지의 배경 제거, 픽셀화, 색상 축소, 애니메이션 리깅, 아틀라스 패킹과 같은 후처리는 로컬 프로그램이 정해진 규칙에 따라 수행한다. 웹 게임의 경제 계산, 이슈 결과, 캐릭터 이동, 엔딩 판정 역시 게임 실행 중 인공지능이 판단하는 것이 아니라 TypeScript로 작성된 조건과 수식에 따라 동작한다.


## 2. 프로젝트 개요

「딸깍 서바이벌」은 1인 개발자가 서비스를 운영하면서 발생하는 여러 문제를 해결하고, 돈과 체력, 사용자 수, 서비스 품질 등의 지표를 관리하는 픽셀 스타일 웹 게임이다.

플레이어는 게임 중 발생하는 개발, 제품, 마케팅, 운영 관련 이슈를 확인하고 선택지 중 하나를 고른다. 선택에는 돈과 체력, 시간이 사용될 수 있으며 성공 여부에 따라 품질과 사용자 수, 매출 등이 변한다. 보유한 돈이나 체력이 모두 소진되면 게임이 종료되고 조건에 맞는 엔딩 화면이 표시된다.

프로젝트는 크게 에셋 생성기와 웹 게임으로 나뉜다. 에셋 생성기는 캐릭터, 사물, 아이콘, 이펙트, 타일, 배경 등의 이미지를 만들고 게임에서 사용할 수 있는 형태로 가공한다. 웹 게임은 생성된 이미지와 기획 데이터를 바탕으로 실제 게임 화면, 이동, 이슈, 경제, UI, 엔딩 등을 처리한다.


## 3. 사용한 인공지능 도구

이미지 생성에는 OpenAI Codex CLI에서 제공하는 이미지 생성 도구를 사용하였다. 사용자가 캐릭터나 사물의 생김새를 입력하면 프로그램이 이를 픽셀 게임에 적합한 프롬프트로 변환하고, Codex를 통해 원본 이미지를 생성한다. 기존 이미지의 일부를 수정하거나 같은 캐릭터의 다른 방향과 동작을 만들 때는 기존 이미지를 레퍼런스로 제공하였다.

프로그램 개발 과정에서도 Codex 계열의 AI 코딩 도구를 사용하였다. 자연어로 전달된 요구사항을 클래스와 모듈 구조로 정리하고, Python과 TypeScript 코드의 초안을 작성하며, 오류가 발생한 경로를 분석하고, 테스트와 문서를 작성하는 용도로 활용하였다.

AI는 프로젝트 기획을 최종 결정하는 역할로 사용하지 않았다. 요구사항의 우선순위, 생성 이미지의 최종 선택, 기능의 승인, Git 커밋과 배포 여부는 사람이 결정하였다.


## 4. 에셋 생성기에서의 AI 활용

### 4.1 에셋 생성기의 목적과 전체 과정

에셋 생성기는 자연어로 작성한 이미지 설명을 실제 게임에서 사용할 수 있는 픽셀 에셋으로 변환하기 위해 제작하였다.

사용자가 만들고 싶은 대상의 생김새를 입력하면 프로그램이 공통 스타일과 카테고리별 규칙을 조합하여 최종 프롬프트를 작성한다. 작성된 프롬프트는 Codex에 전달되며, Codex는 이미지 생성 도구를 이용하여 원본 이미지를 만든다. 이후 원본 이미지의 배경을 제거하고, 목표 크기로 축소하고, 색상 수를 제한하고, 픽셀 외곽선을 만든다. 캐릭터 애니메이션이 필요한 경우 생성된 프레임을 분리하거나 기존 픽셀을 이동하여 추가 프레임을 만든다. 마지막으로 완성된 에셋들을 하나의 아틀라스로 묶고 오류 여부를 검사한다.

전체 과정 중 생성형 AI가 직접 사용되는 부분은 원본 이미지 생성과 기존 이미지 수정 단계이다. 그 이후 단계는 동일한 입력에 동일한 결과를 만드는 일반 이미지 처리 알고리즘이다.


### 4.2 프롬프트 자동 작성

사용자는 에셋 생성 화면에 에셋 종류, 이름, 생김새 등의 정보를 입력한다. 프로그램은 사용자가 입력한 설명을 그대로 AI에 전달하지 않고, 미리 작성된 공통 규칙과 카테고리별 템플릿을 결합하여 최종 프롬프트를 만든다.

픽셀 게임에서는 최종 이미지 크기가 작기 때문에 가는 선이나 작은 장식이 축소 과정에서 사라지기 쉽다. 이를 방지하기 위해 프롬프트에는 지나치게 작은 디테일, 사진식 노이즈, 그라데이션, 블룸, 부드러운 가장자리 등을 억제하는 내용이 포함되어 있다. 캐릭터는 큰 머리와 짧은 몸을 가진 SD 형태로 생성하도록 하였으며, 얼굴도 작은 픽셀에서 알아볼 수 있도록 단순하고 굵은 형태를 요구하였다.

애니메이션 시트를 생성할 때는 행과 열, 프레임 수를 지정하고 모든 프레임에서 캐릭터의 크기, 색상, 디자인과 위치가 최대한 동일하게 유지되도록 지시하였다. 캐릭터가 아닌 전체 화면 배경은 화면 가장자리까지 모두 채우고, 캐릭터와 UI가 올라갈 중앙 영역이 복잡해지지 않도록 별도의 프롬프트를 사용하였다.

또한 AI가 이미지 생성 요청을 받고 불필요한 코드나 파일을 작성하지 않도록 이미지 생성 도구를 한 번만 호출하고, 결과 파일을 지정된 위치에 저장한 후 작업을 종료하도록 명령 범위를 제한하였다.


### 4.3 이미지 생성과 실행 기록

에셋 생성기는 Codex를 별도의 프로세스로 실행하여 이미지를 생성한다. 이미지 생성은 수분이 걸릴 수 있으므로 프로그램 화면이 멈추지 않도록 비동기 방식으로 처리하였다. 생성 진행 상황은 사용자가 읽기 쉬운 문장으로 화면에 표시되며, 상세 실행 이벤트는 별도의 기록 파일로 저장할 수 있도록 구성하였다.

생성 도중 오류가 발생하거나 제한 시간을 넘기면 실행 중인 프로세스를 종료한다. 이를 통해 사용자가 다시 생성을 시도했을 때 두 작업이 동시에 같은 이미지 파일을 수정하는 상황을 방지하였다. AI가 이미지를 예상한 위치가 아닌 다른 임시 위치에 저장한 경우에는 실행 기록에서 이미지 경로를 찾아 원래 작업 폴더로 복구하는 기능도 포함하였다.

AI 이미지 생성은 같은 설명을 사용하더라도 결과가 달라질 수 있다. 따라서 기존 결과를 즉시 덮어쓰지 않고, 생성할 때마다 번호가 부여된 후보로 보관하도록 하였다. 사용자는 여러 후보를 비교한 후 원하는 이미지를 채택할 수 있으며, 과거 후보로 되돌릴 때는 AI를 다시 호출하지 않고 배경 제거와 픽셀화 과정만 다시 실행한다.


### 4.4 레퍼런스 이미지 활용

새 이미지를 만들 때 사용자가 보유한 이미지나 이전에 생성한 에셋을 레퍼런스로 사용할 수 있도록 하였다. 레퍼런스 이미지는 업로드한 파일, 컴퓨터의 특정 경로에 있는 파일, 기존 에셋, 이전 생성 후보 중에서 선택할 수 있다.

캐릭터 제작에서는 최초 정면 이미지가 전체 디자인의 기준이 된다. 옆모습과 뒷모습은 정면 이미지를 AI에 함께 제공하여 머리 모양, 의상, 색상과 같은 특징을 유지하면서 방향만 변경하도록 하였다. 기존 이미지를 수정할 때도 AI가 현재 이미지를 먼저 확인하게 한 후, 사용자가 지정한 부분만 변경하도록 요청하였다.

그러나 레퍼런스를 사용하더라도 AI가 캐릭터의 세부 비율이나 조명을 완전히 동일하게 유지하지 못할 수 있다. 따라서 최종 결과는 사용자가 직접 비교하고 선택하도록 하였다.


### 4.5 배경 제거

AI가 생성한 원본 이미지에는 에셋의 종류에 따라 단색 배경이 포함될 수 있다. 캐릭터와 사물처럼 투명 배경이 필요한 이미지는 크로마키 방식으로 배경을 제거한다. 피사체의 색상과 배경색이 비슷한 경우에는 피사체 일부가 함께 제거될 수 있으므로 다른 키 컬러로 변경할 수 있도록 하였다.

외부에서 가져온 이미지처럼 일반적인 단색 배경을 가진 경우에는 이미지 가장자리부터 연결된 영역만 제거하는 방식도 제공하였다. 이 방식은 피사체 내부의 밝은 부분까지 잘못 지우는 문제를 줄인다. 타일과 전체 화면 배경은 이미지 전체가 사용 대상이므로 배경 제거를 수행하지 않는다.

배경 제거 후에는 남은 불투명 픽셀의 밀도를 검사한다. 피사체가 배경색과 함께 과도하게 지워졌을 가능성이 있으면 프로그램이 경고를 표시하고, 사용자가 다른 배경색으로 다시 생성할 수 있도록 안내한다.

이 배경 제거 과정은 새로운 그림을 생성하는 AI 기능이 아니라 정해진 규칙에 따라 이미지를 처리하는 로컬 프로그램 기능이다.


### 4.6 픽셀화와 색상 처리

배경이 제거된 이미지는 게임에 맞는 작은 크기의 픽셀 이미지로 변환된다. 먼저 피사체가 존재하는 영역을 기준으로 불필요한 여백을 줄이고, 원래 가로세로 비율을 유지하면서 목표 크기로 축소한다. 그 후 반투명 픽셀을 정리하여 가장자리가 흐려지는 것을 방지하고, 사용할 수 있는 색상 수를 제한한다.

색상 축소에는 불투명한 픽셀만 사용하며 디더링을 적용하지 않는다. 디더링은 작은 이미지에 많은 점무늬를 만들 수 있기 때문이다. 캐릭터와 사물은 축소가 끝난 후 실루엣 바깥에 선명한 1픽셀 외곽선을 생성한다.

생성 단계에서부터 굵은 외곽선을 그리지 않고 픽셀화 단계에서 새로 만드는 이유는, 큰 이미지에 그려진 선이 축소 과정에서 흐릿해질 수 있기 때문이다. 목표 해상도로 축소한 후 외곽선을 만들면 모든 에셋에서 선의 굵기를 일정하게 유지할 수 있다.

전체 화면 배경은 기본적으로 256×256 크기와 32색으로 처리한다. 타일과 배경은 투명 영역이 필요하지 않기 때문에 이미지 전체를 목표 크기에 맞게 변환한다.


### 4.7 캐릭터 애니메이션 처리

캐릭터의 큰 동작이나 방향 전환은 여러 프레임으로 구성된 이미지를 AI가 생성하도록 하였다. 이후 프로그램이 각 칸을 개별 프레임으로 분리하고, AI가 프레임마다 캐릭터 위치를 조금씩 다르게 그린 경우 중심이나 발 위치를 기준으로 흔들림을 보정한다.

호흡이나 작은 걷기 동작처럼 단순한 움직임은 AI를 반복 호출하지 않고 이미 완성된 픽셀 이미지의 일부분을 정수 픽셀만큼 이동하여 만든다. 예를 들어 호흡 동작은 다리를 고정한 채 상체 부분을 1~2픽셀 이동하여 표현할 수 있다.

이 방식을 사용하면 AI가 프레임마다 캐릭터를 다시 그리면서 머리카락의 폭이나 신체 비율을 다르게 만드는 현상을 줄일 수 있다. 또한 이미 존재하는 픽셀을 정수 단위로 이동하기 때문에 이미지가 다시 보간되지 않고 외곽선도 흐려지지 않는다.

프로그램은 캐릭터 이미지의 밝기를 분석하여 상체와 양쪽 다리 영역을 자동으로 추정한다. 자동 추정이 실패하면 잘못된 애니메이션을 그대로 만들지 않고 처리를 중단하며, 사용자가 영역 좌표를 직접 지정할 수 있도록 하였다.


### 4.8 아틀라스 제작과 검증

완성된 캐릭터, 사물, 아이콘과 이펙트는 게임에서 효율적으로 불러올 수 있도록 하나의 큰 아틀라스 이미지로 합친다. 각 에셋은 투명하지 않은 실제 영역만 잘라 배치하지만, 원래 캔버스에서의 위치 정보를 JSON 파일에 함께 기록하여 게임 화면에서 원래 위치를 복원할 수 있도록 하였다.

아틀라스 배치는 크기와 이름에 따라 일정한 순서로 처리되므로 같은 입력을 사용하면 같은 결과가 생성된다. 각 프레임 가장자리에는 1픽셀 확장 영역과 여백을 추가하여 이미지를 확대하거나 축소할 때 옆 프레임의 색상이 섞이는 문제를 줄였다.

빌드 과정에서는 프레임 이름 중복, 애니메이션 이름 중복, 이미지 범위를 벗어난 프레임, 서로 겹치는 프레임, 완전히 투명한 프레임, 이미지와 JSON 크기의 불일치 등을 검사한다. 타일 이미지는 반복 배치했을 때 경계가 눈에 띄는지 추가로 검사한다.

타일과 전체 화면 배경은 각각 독립적인 이미지로 사용해야 하므로 일반 스프라이트 아틀라스에 넣지 않고 별도 폴더로 내보낸다.


### 4.9 사람의 검토 과정

에셋 생성기는 AI가 만든 이미지를 자동으로 최종 확정하지 않는다. 사용자는 원본 이미지, 배경 제거 결과, 픽셀화 결과를 나란히 확인할 수 있다. 캐릭터 애니메이션은 실제 재생 속도의 GIF로 확인하여 걷기 동작이 미끄러져 보이지 않는지, 프레임 전환이 지나치게 빠르지 않은지 검토한다.

같은 설명으로 여러 번 생성한 후보도 한 화면에서 비교할 수 있으며, 사용자가 선택한 후보만 현재 버전으로 채택한다. 정면 캐릭터가 만족스럽지 않으면 옆모습과 뒷모습을 만들기 전에 먼저 정면을 다시 생성하는 방식으로 불필요한 AI 호출을 줄였다.

따라서 AI는 원본 시안을 빠르게 만드는 역할을 담당하고, 실제 게임에 사용할 이미지의 최종 선택과 품질 판단은 사람이 담당한다.


## 5. 웹 게임 개발에서의 AI 활용

### 5.1 기본 개발 환경과 에셋 로딩

웹 게임은 TypeScript, HTML Canvas, DOM, CSS와 Vite를 사용하여 제작하였다. AI 코딩 도구는 프로젝트의 초기 구조, 빌드 설정, 이미지와 JSON 로더, 아틀라스 프레임 및 애니메이션 로더를 구성하는 데 사용하였다.

아틀라스에 실제로 존재하는 프레임과 애니메이션 이름을 TypeScript 타입으로 자동 생성하도록 하였다. 이를 통해 코드에서 존재하지 않는 이미지 이름을 입력하는 실수를 빌드 전에 발견할 수 있다.


### 5.2 시간, 자원과 경제 시스템

게임 시간, 고정 시드 난수, 보유 금액, 체력, 사용자 수, 품질, 매출, 서버비, 체력 회복과 성공 확률 계산을 각각 독립적인 모듈로 작성하였다. AI는 이 구조를 설계하고 반복적인 계산 코드를 작성하며 오류 가능성을 점검하는 데 활용하였다.

계산 기능을 화면 코드와 분리하여 같은 입력에 같은 결과가 나오도록 하였으며, 각 계산 규칙을 다른 기능과 독립적으로 확인할 수 있게 하였다. 게임 실행 중에는 AI가 경제 결과를 계산하지 않는다. 모든 결과는 프로그램에 작성된 수식으로 결정된다.


### 5.3 이슈와 선택지 시스템

게임에서 발생하는 문제는 개발, 제품, 마케팅, 운영 분야로 나누어 클래스 단위로 작성하였다. 각 이슈는 제목, 설명, 선택지, 필요한 돈과 체력, 소요 시간, 성공 확률, 성공 보상과 방치 효과를 가진다.

AI 코딩 도구는 기획 내용을 공통 구조와 개별 이슈 클래스로 정리하고, 반복되는 구현을 작성하는 데 사용하였다. 공통적인 성공·실패 판정은 기반 클래스에서 처리하며, 각 이슈의 차이는 별도의 수치와 조건으로 정의하였다.

사용자가 선택지를 고른 후의 성공과 실패는 코드에 작성된 확률과 고정 난수 생성기를 사용하여 결정된다. AI가 플레이어의 선택을 평가하거나 결과를 임의로 작성하지 않는다.


### 5.4 방과 캐릭터 이동

AI 코딩 도구의 도움을 받아 고정 크기의 방 좌표계, 정수 배율 확대, 마우스와 터치 좌표 변환, 사물의 충돌 영역, 사물 앞에 설 위치, 격자 기반 길 찾기, 캐릭터 방향과 애니메이션 변경 기능을 구현하였다.

사용자가 방 안의 사물을 클릭하면 캐릭터는 장애물을 피해 해당 사물 앞으로 이동한다. 사물 앞에 도착하면 상호작용 함수가 실행된다. 작업 책상은 이슈 선택 결과 처리와 연결되어 있다.

현재 문, 냉장고, 침대와 같은 일반 사물은 클릭과 이동까지만 구현되어 있다. 캐릭터가 도착하면 개발자 콘솔에 상호작용 기록이 출력되지만, 말풍선, 대사, 체력 회복, 시간 소모와 같은 실제 행동은 아직 연결되지 않았다. 이 부분은 상호작용 구조만 준비된 상태이며 완성된 콘텐츠로 보기는 어렵다.


### 5.5 게임 세션과 화면 UI

게임의 전체 상태는 Session 객체가 관리하도록 설계하였다. 여기서 Session은 서버의 TCP 연결을 의미하는 것이 아니라 한 번의 게임 플레이를 관리하는 게임 관리자 역할의 객체이다.

Session은 시계, 플레이어 상태, 제품 상태, 이슈, 월드, HUD, 알림과 엔딩 관리자를 소유한다. 돈이나 체력, 시간과 같은 값은 Session을 통하여 변경하고, 변경된 상태가 화면에 바로 반영되도록 하였다.

AI는 이 객체 구조를 정리하고 HUD, 이슈 선택 화면, 알림 메시지, 캔버스와 게임 상태를 연결하는 코드를 작성하는 데 사용하였다. 게임 실행 중 Session의 판단은 모두 코드에 작성된 규칙으로 이루어진다.


### 5.6 엔딩 시스템

기획 자료에서 돈이나 체력이 바닥나면 더 이상 서비스를 운영할 수 없다는 조건을 확인하고, AI 코딩 도구를 활용하여 1차 엔딩 시스템을 구현하였다.

엔딩의 공통 형태는 Ending 클래스에 정의하고, 실제 조건은 파산 엔딩과 번아웃 엔딩 클래스로 분리하였다. EndingManager는 현재 게임 상태를 받아 각 조건을 우선순위대로 검사하고, 한 번 결정된 엔딩 결과가 이후에 변경되지 않도록 보관한다. Session은 돈이나 체력이 변하는 시점에 엔딩 조건을 검사하며, 조건이 충족되면 게임 진행을 멈추고 엔딩 화면을 표시한다.

현재 파산 엔딩은 보유 금액이 0 이하일 때 발생한다. 번아웃 엔딩은 체력이 0 이하일 때 발생한다. 돈과 체력이 동시에 0 이하가 되면 현재 구현에서는 파산 엔딩이 먼저 선택된다. 이 우선순위는 임시로 적용한 것이며, 최종 기획이 정해지면 별도의 복합 엔딩이나 다른 우선순위로 변경할 수 있다.

선택지 비용으로 돈이 정확히 0이 된 직후에는 이후의 매출 정산으로 금액이 다시 증가하기 전에 엔딩 조건을 먼저 검사한다. 이를 통해 돈을 모두 사용했음에도 파산 엔딩이 발생하지 않는 문제를 방지하였다.

엔딩 판정 과정에서도 생성형 AI는 사용되지 않는다. 돈과 체력 값을 확인하는 일반 조건문으로 처리된다.


## 6. 문서 작성과 개발 환경 구성에서의 AI 활용

AI 코딩 도구는 프로그램 코드뿐 아니라 프로젝트 사용법과 개발 문서를 작성하는 데에도 사용하였다. 프로젝트의 기본 설명, 로컬 실행 방법, 현재 구현된 기능과 미구현 기능을 README 문서로 정리하였다.

또한 개발 경험이 없는 디자이너가 에셋 생성기를 사용할 수 있도록 macOS 기준 설치 및 실행 안내서를 작성하였다. Node.js, pnpm, uv와 Codex CLI 설치 방법, ChatGPT 계정 로그인 방법, 에셋 생성기 실행 순서, 결과물 위치와 오류 발생 시 전달해야 할 내용을 문서화하였다. 디자이너가 전달받은 압축 파일을 풀고 VS Code에서 바로 확인할 수 있도록 실행 스크립트와 전달용 압축 파일을 구성하는 과정에도 AI의 도움을 받았다.

AI는 설치 명령과 절차를 안내하였지만 ChatGPT 계정 로그인과 인증은 사용자가 직접 수행한다. 비밀번호나 인증 정보는 프로젝트 저장소에 기록하지 않았다.


## 7. 사람과 AI의 역할 구분

프로젝트의 기획 방향, 필요한 기능, 구현 우선순위와 최종 결과물은 사람이 결정하였다. 에셋의 생김새를 설명하고 여러 생성 후보 중 사용할 이미지를 선택하는 일도 사람이 수행하였다. 실제 게임을 실행하여 캐릭터 이동, UI, 체력 엔딩과 파산 엔딩이 정상적으로 보이는지 확인하였으며, 코드의 커밋과 원격 저장소 반영 여부 역시 사용자의 승인 후 진행하였다.

AI는 자연어 요구사항을 프로그램 구조로 변환하고, Python과 TypeScript 코드의 초안을 작성하며, 반복 작업을 줄이는 역할을 하였다. 또한 코드 흐름을 검색하여 누락된 조건을 찾고, 타입 검사와 빌드 결과를 확인하고, 사용법과 보고서 문장을 작성하는 데 활용되었다.

배경 제거, 픽셀화, 색상 축소, 애니메이션 리깅, 아틀라스 제작, 게임 경제와 엔딩 계산은 자동 프로그램이 담당한다. 이러한 자동 프로그램은 AI의 도움으로 작성되었을 수 있으나, 실행 시 AI 모델에 질문하거나 결과를 추론하지 않는다. 입력값과 정해진 규칙에 따라 로컬에서 계산한다.


## 8. 검증 및 품질 관리

AI가 생성하거나 작성한 결과는 그대로 최종 결과로 사용하지 않고 단계별로 검토하였다.

에셋 생성 과정에서는 원본 이미지, 배경 제거 결과, 픽셀화 결과를 비교하였다. 여러 생성 후보를 보존하여 이전 결과로 돌아갈 수 있도록 하였고, 애니메이션은 실제 재생 속도의 GIF로 확인하였다. 최종 빌드 단계에서는 타일 경계, 아틀라스의 프레임 범위와 겹침, 투명 프레임, 메타데이터 오류를 검사하였다.

웹 게임은 TypeScript 타입 검사와 Vite 배포 빌드를 실행하여 기본적인 코드 오류를 확인하였다. 아틀라스 데이터와 코드에서 사용하는 이미지 이름도 함께 검사하였다. 엔딩 시스템은 정상 상태, 돈 0, 체력 0, 돈과 체력이 동시에 0인 상태를 확인하였으며, 엔딩이 결정된 이후 결과가 변경되지 않는지도 점검하였다.

다만 타입 검사와 빌드 통과가 모든 기획 의도와 플레이 경로를 보장하는 것은 아니다. 실제 콘텐츠와 수치 균형은 계속해서 사람이 플레이하며 확인해야 한다.


## 9. AI 활용의 한계

생성형 AI는 같은 프롬프트를 다시 사용하더라도 동일한 이미지를 만들지 않을 수 있다. 애니메이션 시트에서는 프레임마다 신체 비율, 실루엣, 조명이나 소품 위치가 달라질 수 있으며, 작은 장식과 가는 선은 픽셀 크기로 축소한 후 사라질 수 있다. 피사체와 배경의 색이 비슷하면 배경 제거 과정에서 피사체 일부가 함께 지워질 수도 있다.

이러한 문제를 줄이기 위해 프롬프트에 형태와 색상 제한을 포함하고, 후보 이력을 보존하며, 일부 애니메이션은 AI 생성 대신 정수 픽셀 리깅으로 제작하였다. 그러나 최종 이미지의 품질과 사용 가능 여부는 반드시 사람이 확인해야 한다.

코드 작성 보조에서도 AI는 전체 기획 의도를 자동으로 알 수 없다. 기획에 명확하게 적혀 있지 않은 경우 임시 규칙을 제안할 수 있다. 현재 돈과 체력이 동시에 0이 되었을 때 파산을 우선하는 규칙이 그 예이다. 일반 사물 상호작용 역시 구조는 작성되었지만 실제 대사와 효과는 아직 구현되지 않았다.


## 10. 생성 이력과 출처 기록의 한계

에셋 생성기는 각 이미지 생성 작업의 프롬프트, 생성 시각, 수정 요청, 후보 이미지와 실행 이벤트를 기록할 수 있도록 만들어졌다. 이러한 기록을 함께 보관하면 어떤 입력을 사용해 어떤 결과를 만들었는지 확인할 수 있다.

그러나 현재 Git 저장소에는 모든 개별 생성 작업의 후보 폴더와 실행 이벤트가 포함되어 있지 않다. 따라서 저장소에 포함된 아틀라스와 타일 이미지 각각이 정확히 어떤 모델 호출에서 생성되었는지까지 현재 문서만으로 개별 증명하기는 어렵다.

저장소에서 확인할 수 있는 내용은 에셋 생성기가 Codex의 이미지 생성 기능을 사용하도록 구현되어 있다는 점, 이미지 생성 후의 픽셀 처리 과정은 로컬 알고리즘이라는 점, 웹 게임이 완성된 아틀라스와 타일을 사용한다는 점이다.

향후 생성 출처를 더 정확하게 제출해야 할 경우 최종 채택한 이미지의 프롬프트, 생성 시각, 도구 버전, 원본 이미지 해시와 실행 기록을 별도로 보관하고, 최종 빌드 결과와 연결되는 목록을 함께 작성하는 것이 적절하다.


## 11. 개인정보, 저작권 및 보안 고려사항

이미지 생성 프롬프트와 레퍼런스에는 개인정보, 비밀번호, API 키 또는 공개할 필요가 없는 내부 정보를 입력하지 않도록 주의하였다. 레퍼런스 이미지는 직접 제작하였거나 사용 권한을 확인할 수 있는 자료를 사용해야 한다.

특정 작가의 고유 화풍을 그대로 복제하도록 요청하기보다는 픽셀 크기, 색상 수, 형태, 카메라 시점과 같은 일반적인 시각 특성을 설명하는 방식으로 프롬프트를 구성하였다. 생성된 결과가 기존 캐릭터, 상표 또는 저작물과 지나치게 비슷하지 않은지도 실제 사용 전에 사람이 확인해야 한다.

Codex의 로그인 정보는 사용자의 로컬 인증 영역에서 관리한다. 로그인 정보와 비밀번호는 코드, Git 저장소 또는 디자이너 전달용 압축 파일에 포함하지 않는다. 생성 기록을 외부에 제출할 때에는 파일 안에 컴퓨터 사용자명이나 절대 경로가 포함되어 있는지 먼저 확인해야 한다.


## 12. 결론

「딸깍 서바이벌」 프로젝트에서는 인공지능을 원본 이미지 생성과 프로그램 개발 보조에 활용하였다. 이미지 생성 과정에서는 AI가 빠르게 시안을 만들 수 있다는 장점을 활용하고, 프롬프트 규칙, 후보 이력, 단계별 미리보기와 사람의 검토를 통해 결과의 불확실성을 관리하였다.

AI가 생성한 원본은 그대로 사용하지 않고 배경 제거, 픽셀화, 색상 축소, 외곽선 생성, 애니메이션 리깅, 아틀라스 패킹과 검증 과정을 거쳤다. 이 후처리 과정은 동일한 입력에 동일한 결과를 만드는 로컬 알고리즘으로 구현하였다.

웹 게임 개발에서는 AI가 모듈 구조 설계, 반복 코드 작성, 오류 분석, 테스트와 문서 작성을 보조하였다. 그러나 실제 게임에서 경제, 이슈, 캐릭터 이동과 엔딩을 결정하는 것은 명시적으로 작성된 프로그램 코드다.

최종적으로 본 프로젝트의 AI 활용 방식은 AI가 모든 결과를 자동으로 결정하는 형태가 아니라, AI가 이미지 생성과 개발 작업을 보조하고 사람이 결과를 검토하여 선택하는 협업 방식이라고 정리할 수 있다.
