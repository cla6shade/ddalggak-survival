# Python
.venv/
__pycache__/
*.py[cod]

# Node
node_modules/

# 생성된 스프라이트와 아틀라스. 파이프라인이 다시 만들 수 있고, 121MB 는 git 이
# 감당할 크기가 아닙니다. web/public/assets/ 에 실제로 쓰는 것만 들어갑니다.
asset-generator/assets/
asset-generator/dist/

# 무엇을 만들지 적어둔 목록. 만드는 사람의 작업 파일이라 공개하지 않습니다.
asset-generator/catalog.toml

# codex 를 띄울 때 생기는 브라우저 프로필
asset-generator/chrome-profile/

# 빌드 산출물과 vite 가 남기는 캐시
web/dist/
.vite

# 시뮬레이터가 뱉은 판 기록. 돌리면 다시 나오고, 판마다 수백 KB 씩 쌓입니다.
# 회귀 기준인 simulator/test/golden.json 은 여기 해당하지 않습니다.
simulator/runs/

# Arcweave 에서 뽑은 시나리오 원본. 빌드도 런타임도 읽지 않고, 이슈 정의는
# web/src/game/issues/ 로 이미 옮겨 적었습니다.
scenario.json

# macOS
.DS_Store
