LINE ART (critical, read carefully): draw this WITHOUT any outline. There must be NO black contour line, NO dark keyline, NO ink stroke, and NO border of any kind — neither around the outside silhouette nor between internal parts. This is a lineless / line-free flat illustration. Every shape is defined purely by its own solid fill color. The outline is added later by a separate program; drawing one here would be destroyed by downscaling.

Because there are no lines, adjacent shapes MUST be separated by strong value and hue contrast instead. Any two shapes that touch must differ sharply in brightness so the form still reads without a line between them.

Shading (critical): flat cel shading with at most {{value_steps}} tones per material — one flat base fill plus at most {{shadow_steps}} darker shade. No highlights, no rim light, no ambient occlusion, no third intermediate tone. In particular, large light areas must be one single flat fill plus at most one hard-edged shadow shape — never a spray of several near-identical tones. Every shading boundary is hard-edged, never soft.

Detail budget (critical): each final sprite is only {{size}}x{{size}} pixels, so any feature thinner than roughly one twentieth of the image will vanish. {{detail_ban}}

{{background_rule}}

Do not include: outline, contour line, keyline, cast shadow, contact shadow, reflection, watermark, text, logo, speech bubble, border, frame{{grid_ban}}.

Size: 1024x1024 square.
